// Resumen / Mando — KPIs (capital, revenue, ganancia, stock), charts, período.
// FASE 4: implementación completa con KPIs verificados vs SQL.
export default function OverviewPage({ period }) {
  return (
    <div>
      <div className="topbar"><div><h1>Resumen</h1><div className="sub">Panel de mando · período: {period}</div></div></div>
      <div className="section"><div className="empty">Mando — pendiente de implementar (Fase 4)</div></div>
    </div>
  );
}
