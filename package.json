{
  "name": "jamcs-tech-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "JAMC's Tech terminal dashboard — Bloomberg-themed React/JSX dashboard sirviendo desde Netlify (sin build step en cliente; Babel-standalone). Este package.json existe solo para devDependencies de testing.",
  "scripts": {
    "test": "playwright test",
    "test:usuario": "playwright test tests/persona-usuario.spec.js",
    "test:qa": "playwright test tests/persona-qa.spec.js",
    "test:qaui": "playwright test tests/persona-qaui.spec.js",
    "test:tester": "playwright test tests/persona-tester.spec.js",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "netlify-cli": "^26.0.2"
  }
}
